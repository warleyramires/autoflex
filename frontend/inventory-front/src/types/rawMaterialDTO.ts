export type CreateRawMaterialDTO = {
    name: string;
    code: string;
    stockQuantity: number;
    unit: string;
}

export type UpdateRawMaterialDTO = {
    name: string;
    code: string;
    stockQuantity: number;
    unit: string;
}